@*@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUserRole>

@{
    ViewData["Title"] = "Index";
}

<div class="main-container" id="container">
    <partial name="_StaffDashboardPartial" />
    <!-- BEGIN CONTENT AREA -->
    <div id="content" class="main-content">
        <div class="layout-px-spacing">
            <div class="row layout-spacing">
                <!-- Content -->
                <div class="col-xl-10 col-lg-10 col-md-10 col-sm-12 mx-auto layout-top-spacing">
                    <a asp-action="back" class="btn btn-dark mb-1">Create New</a>
                    <h2>Room Details</h2>
                    <div class="widget-content widget-content-area br-6">
                        <table id="html5-extension" class="table table-hover non-hover">
                            <thead>
                                <tr>
                                    <th>
                                        User
                                    </th>
                                    <th>
                                        Role
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @{
                                                // Retrieve the username associated with the UserId
                                                var user = await _userManager.FindByIdAsync(item.UserId);
                                                var username = user?.UserName ?? "User Not Found";
                                                @username
                                            }
                                        </td>
                                        <td>
                                            @{
                                                // Retrieve the role name associated with the RoleId
                                                var role = await _roleManager.FindByIdAsync(item.RoleId);
                                                var roleName = role?.Name ?? "Role Not Found";
                                                @roleName
                                            }
                                        </td>
                                        <td>
                                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- END CONTENT AREA -->
</div>
<!-- END MAIN CONTAINER -->
*@